# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
coverage/
.nyc_output/

# Playwright
playwright-report/
test-results/
playwright/.cache/

# Uploads
uploads/
temp/

# Database
*.db
*.sqlite
# Redis 落盘快照。开发时跑 redis-server 会在工作目录生成，里面是真实缓存内容。
dump.rdb

# Playwright seed 产物。e2e/seed.ts 把真实 accessToken / refreshToken / userId
# 写进这个文件。曾经被跟踪过，历史里泄漏了一个真实 userId（7fda4c2）；
# 现已取消跟踪，不要再加回来。
e2e/.auth-state.json

# 临时备份。改大文件前手抖 cp 出来的，不该进版本库——版本库本身就是备份。
*.backup
*.bak
*.orig

# Worktrees
.worktrees/

# 样板面（Market 沉浸叙事）的一次性评估产物，不是回归测试的一部分：
#   .pilot-art/     — curl 下来的 Unsplash 人像，第三方版权，不入库
#   pilot-shots/    — 截图输出，3MB 二进制
#   e2e/_pilot-*    — 生成上面两者的临时脚本；playwright.config.ts 的
#                     testIgnore 已排除 _ 前缀，跑全量 e2e 时不会被带上
.pilot-art/
pilot-shots/
e2e/_pilot-*.spec.ts

# 根目录的临时工作笔记（ERA 兼容性那轮），结论已经写进 commit message
# 和 docs/improvements/，留在磁盘上仅供回看
/findings.md
/progress.md
/task_plan.md
/image.png
